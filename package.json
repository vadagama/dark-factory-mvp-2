{
  "name": "dark-factory-mvp-2",
  "version": "0.1.0",
  "private": true,
  "description": "Dark Factory v4 — TypeScript-ядро, DSH как движок исполнения, Spec Kit как SDD-слой",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "~5.9.2",
    "vitest": "^3.2.4"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
